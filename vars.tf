variable "parameters" {
  description = "A map of SSM parameter configurations, where each key represents the parameter name, and each value is an object containing 'type' (e.g., 'String', 'SecureString') and 'value' for the parameter."
}
